streamlit>=1.35,<2
pandas>=2.1,<3
numpy>=1.26,<3
vaderSentiment>=3.3.2
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
matplotlib>=3.8.0
pyyaml>=6.0
# The deployed dashboard renders generated report artefacts; it never fits a
# Bayesian model at request time, so PyMC/ArviZ are deliberately NOT installed
# here. They live in the optional `forecast` extra used by the experiment
# scripts: pip install -e .[forecast]
